<!--
title: The Modality-Asymmetric Teacher — Conditioning-Richness 노브로 VLM 증류의 grounded-uncertainty 붕괴를 vision encoder에 인과적으로 국소화
topic: vision-language model off-policy vs on-policy distillation
reviewer_avg: 7.00 / 10   (4 reviewers, all weak_accept)
reviewer_min: 7
novelty_axis: 8.00 / 10
fatal_flaws: 0
pass_strict_bar: NO (avg 7.0 < 7.5) — clean weak-accept, best-effort 산출물
generated_by: /novel-paper-idea (workflow wf_1ff7437f-e25, 134 agents; 워크플로 후반 parallel 단계 크래시로 결과를 트랜스크립트에서 복구함)
citations_verified: 2026-06-05 (arXiv 2603.24472, 2504.03440, 2602.21054, 2310.08255 — 4/4 실재 확인)
-->

# 모달리티-비대칭 교사(The Modality-Asymmetric Teacher)
### Conditioning-Richness 노브로 VLM 증류가 일으키는 "근거 기반 불확실성 붕괴"를 시각 인코더에 인과적으로 국소화하기

> ⚠️ **솔직한 위치 고지**: 이 아이디어는 4명의 리뷰어 전원에게서 **weak-accept(평균 7.0 / 신규성 8.0 / 최저 7 / 치명적 결함 0)** 를 받았습니다. 스킬의 엄격 통과선(평균 ≥ 7.5)에는 **0.5점 못 미칩니다.** 즉 "강한 통과"가 아니라 **깔끔하게 받아들일 만한, 다듬으면 통과 가능한 best-effort 산출물**입니다. (워크플로가 후반 라운드의 한 단계에서 크래시했기 때문에, 자동 합성 대신 제가 트랜스크립트에서 가장 점수가 높은 후보를 복구해 작성했습니다.)

---

## 한눈에 보기 (2분 직관)

**한 줄 ELI5.** "선생님이 깨끗한 사진을 보면서 가르치고, 학생은 흐릿한 사진만 보고 시험을 본다." 이렇게 가르치면(증류하면) 학생 모델은 *자기가 사실은 못 보는 것*에 대해서도 자신만만하게 답하게 됩니다. 이 논문은 **그 자신감이 정확히 신경망의 어디서 망가지는지를, 흐림의 강도를 다이얼처럼 돌려가며 인과적으로 집어내는** 연구입니다.

**비유.** 안과 의사(교사)가 선명한 검사 차트를 보면서 수련의(학생)를 가르칩니다. 그런데 수련의는 실전에서 김 서린 안경을 끼고 환자를 봅니다. 교사를 그대로 흉내 내도록 훈련하면, 수련의는 "잘 안 보이는데요"라고 말해야 할 상황에서도 교사처럼 단호하게 진단을 내립니다. **안경의 김 서림 정도(흐림 강도)를 0%에서 100%까지 돌릴 수 있다**는 점이 핵심입니다 — 이건 글(텍스트) 모델에는 없는, 오직 "이미지"라는 입력이 있어서 생기는 실험 손잡이입니다.

**핵심 통찰.** 최근 2026년 텍스트 LLM 연구(arXiv:2603.24472)가 밝힌 사실: 자기증류가 추론 능력을 해치는 진짜 원인은 "증류 그 자체"가 아니라 **교사가 얼마나 풍부한 정보를 보고 있었는가(conditioning-richness)** 다. 정보가 풍부한 교사는 "불확실하다"는 말을 억제한 자신만만한 풀이를 만들고, 학생이 이를 모방하면 분포 밖(OOD)에서 무너진다. **VLM에는 텍스트에 없는 특권이 하나 있다**: 교사에게는 *깨끗한 이미지*를, 학생에게는 *손상된 같은 이미지*를 줄 수 있다. 이 "깨끗 vs 손상" 간격이 곧 conditioning-richness를 **연속적으로, 정밀하게 돌릴 수 있는 다이얼(투여량)** 이 된다. → 관찰적(혼란변수 섞인) 질문이 **조작된 용량–반응 인과 실험**으로 바뀐다.

**한 가지 예시 끝까지.** MathVista의 도형 추론 문제. (1) 깨끗한 도형으로 교사가 모범 풀이를 만든다. (2) 학생은 흐림 강도 s만큼 손상된 같은 도형으로 그 풀이를 모방하도록 자기증류된다. (3) s를 0→최대까지 올리면서, 학생이 "확실하다"고 말하는 정도(verbalized confidence)와 OOD 정확도를 측정한다. 예측: s가 커질수록 **근거 기반 불확실성이 단조롭게 붕괴**하고 OOD 정확도가 떨어진다. (4) 그다음 활성값 패칭(activation patching)으로 그 붕괴가 **시각 인코더 / 투영기 / 언어 헤드** 중 어디를 거쳐 일어나는지 인과적으로 분해한다.

```
   [깨끗한 이미지 x0] ──▶ 교사(동일 가중치, grad 없음) ──▶ 모범 트레이스
            │                                                  │
       richness gap = info(x0) − info(x_c)            (학생이 모방)
            │                                                  ▼
   [손상 이미지 x_c, 강도 s] ─▶ 학생(on-policy 롤아웃) ─▶ 자기증류(SFT)
                                                              │
        ┌──────── 활성값 패칭으로 인과 국소화 ────────┐        ▼
        │  시각 인코더?   투영기?   언어 헤드?       │   "근거 기반 불확실성" 붕괴?
        └───────────────────────────────────────────┘   (3가지 지표로 삼각측정)

   대조군:  Arm2(교사·학생 둘 다 x_c → richness gap=0)  ─┐  → 증류-기인 효과
            Base(증류 없이 x_c만)  ←── 알려진 혼란변수    ─┘  = Arm1 − Arm2 − Base
```

**왜 새로운가 (한 줄).** "흐리게 하면 자신감이 떨어진다"는 *뻔한* 관찰이 아니라, **흐림을 용량처럼 투여해 + 알려진 기저 혼란변수를 빼고 + 그 붕괴가 신경망의 어느 부품을 거치는지를 인과적으로 집어낸다** — 이 세 가지를 동시에 한 선행연구는 없다.

---

## 1. 분야와 이 "여백(white space)"이 왜 비어 있는가

자기증류(self-distillation)는 VLM 후처리(post-training)의 표준 레시피지만, 분포 밖(OOD) 강건성을 **조용히** 망가뜨릴 수 있다. 2026년 텍스트 전용 연구(arXiv:2603.24472)가 이 현상의 인과적 원인을 "증류 자체가 아니라 교사의 conditioning-richness"로 규명했지만, 이를 **추론형 VLM으로 이식한 연구는 없다.** VLM 증류 문헌은 추론-OOD를 손상(corruption)-OOD로 바꿔 다루지만, "기저 모델이 증류 없이도 손상 이미지에서 교정된 불확실성을 잃는다"는 **이미 문서화된 혼란변수**(arXiv:2504.03440)를 통제하지 않는다. 그래서 관찰된 붕괴가 증류 때문인지 손상 때문인지 **구분 불가능**한 상태로 남아 있다. 이 통제되지 않은 인과 질문이 바로 비어 있는 여백이다.

## 2. 문제 (왜 중요하고 왜 안 풀렸나) — 구체 예시로 시작

차트 QA 모델을 자기증류로 다듬었더니 사내 벤치는 좋아졌는데, 저해상도 스캔 문서에서 **틀린 답을 자신만만하게** 내놓기 시작했다고 하자. 이때 세 가지가 막혀 있다:

1. **VLM판 "richness 노브"가 무엇인지 아무도 정의하지 않았다.** 텍스트의 "모범 정답(gold solution)"에 대응하는 시각적 손잡이가 무엇인가?
2. **자기증류의 피해가 시각 인코더(텍스트에 없는 부품)에 국소화되는지, 언어 헤드에 국소화되는지**는 그래디언트로 얽혀 있어 상관관계로는 못 가린다.
3. **근거(grounding) 테스트** 자체가 부실하다 — 교차어텐션 하드 마스킹은 "증거 제거"와 "분포 밖 이동"을 뒤섞고, 교차어텐션은 신뢰할 수 없는 증거 위치 추정기다.

## 3. 핵심 통찰

비전은 텍스트가 줄 수 없는 실험 손잡이를 준다: **교사의 조건부 입력(이미지)** 과 **학생의 평가 입력(손상된 같은 이미지)** 을 *정보량 축을 따라 연속적으로* 분리할 수 있다. 깨끗한 이미지는 LLM 교사의 "모범 정답"에 해당하는 VLM 아날로그다 — 학생이 갖지 못할 정보를 교사에게 주어 **동일한 불확실성-억제 메커니즘**을 유발하되, 이번엔 **손잡이(손상 강도)** 가 달려 있다. 여기에 ⑴ 무증류 손상 기저선(알려진 혼란변수 차감)과 ⑵ 시각 인코더를 언어 헤드에서 외과적으로 분리하는 매개 분석을 결합하면, 관찰적 역설이 **반증 가능한 인과-국소화 주장**으로 바뀐다.

## 4. 제안 방법 / 연구 설계 (구체·구현 가능)

- **백본**: 명시적 CoT와 언어화 가능한 불확실성을 가진 오픈 추론형 VLM (예: Qwen2.5-VL-7B-Instruct, InternVL 계열) — 2개 아키텍처 복제.
- **과제**: 이미지를 손상시키면 증거가 단계적으로 사라지되 원리상 답은 가능한 시각 추론 — MathVista, MathVision, ChartQA, ScienceQA-IMG, DocVQA.

**(A) 모달리티-비대칭 on-policy 자기증류.** 학생이 손상 이미지 x_c(강도 s)로 on-policy 롤아웃 생성 → 교사(동일 가중치, grad 없음)가 **깨끗한 이미지 x_0**에 조건화되어 목표 트레이스를 만든다. 시퀀스 수준 자기증류(교사 선호 트레이스에 SFT)로 **폭(width)을 고정** — 원 논문이 연구한 동일 폭 레짐과 일치. 용량 = 손상 강도 s (ImageNet-C 스타일 15종 × 5단계 격자), s=0이면 대칭 자기증류로 복귀.

**(B) 혼란변수를 죽이는 사전등록 대조군 2개.**
- **Arm 1 (다이얼)**: 교사=x_0, 학생=x_c → richness gap = info(x_0) − info(x_c).
- **Arm 2 (동일-richness 플라시보)**: 교사·학생 **둘 다** x_c → richness gap = 0.
- 증류-기인·richness-구동 효과 = **Arm1 − Arm2**. 추가로 **무증류 기저선**(동결 base가 x_c 평가)으로 arXiv:2504.03440의 기저 손상 혼란변수를 포착.

**(C) 랭크 축소를 *가정하지 말고 실증*.** 어떤 국소화 주장보다 먼저, 시각 인코더·투영기·LLM 블록 활성값의 **유효 랭크**(참여 비율 / 99%-에너지 차원)를 증류 전후·용량 격자에서 측정. 시퀀스 수준 자기증류가 실제로 인코더 유효 랭크를 좁히는지 검증 — 메커니즘 서사 전체가 이 통과 여부에 조건부다.

**(D) 메커니즘-정합 인과 매개 국소화.** richness의 총효과를 ⑴ 시각 인코더 ⑵ 투영기 ⑶ 언어 헤드 경로로 활성값 패칭(무증류 활성값을 한 부위씩, 또 결합으로 복원)을 통해 분해. **Shapley 식 가법적 기여**를 보고하고 **가법성(단일 합 vs 결합 패칭)을 명시적으로 검정** — 비가법성은 숨기지 않고 보고.

**(E) 아티팩트에 강건한 근거 테스트.** 교차어텐션 하드 마스킹 대신 **온-매니폴드 증거 제거 탐침**: ⑴ 답 관련 영역을 이미지 매니폴드 위에서 제거하는 생성형 인페인팅 + ⑵ 동일 픽셀 크기의 무관 영역 대조 편집. 근거 = 관련 제거 시 불확실성↑, 크기 맞춤 대조에서는 변화 없음. **VAUQ의 마스킹 Image-Information Score(arXiv:2602.21054)** 로 독립 교차검증하고, 밀도 모델 우도로 오프-매니폴드 진단.

**추정량(estimand).** 1차: Arm1−Arm2에 대한 d(근거 불확실성)/d(richness gap) 용량-반응 기울기 + 시각 인코더가 매개하는 비율. 불확실성은 ⑴ 언어화 신뢰도 교정(ECE) ⑵ 표본 추론들의 의미적 엔트로피 ⑶ VAUQ IS — **3중 측정**으로 단일 지표 아티팩트 회피.

## 5. 선행연구와의 차이 (표절·중복 없음의 증거)

| 선행연구 (검증됨) | 무엇을 하나 | 우리의 델타 |
|---|---|---|
| **arXiv:2603.24472** (원천 메커니즘) | 텍스트 LLM에서 conditioning-richness가 언어화 불확실성을 억제 → OOD 피해 | 동일 *메커니즘*을 추론형 VLM으로 이식. **연속 조작 용량**, 용량-반응 추정량, **텍스트에 없는 신경망 부위(시각 인코더) 국소화** 추가 |
| **arXiv:2504.03440** | 손상 이미지에서 base VLM이 교정 불확실성을 잃음 (증류 無) | 경쟁자가 아니라 **차감해야 할 기저 혼란변수**로 사용 |
| **arXiv:2310.08255** (VL2V-ADiP) | 분류용 도메인 일반화 위해 텍스트 인코더 불변성을 *이미지 인코더로* 증류 | 증류 **방향이 반대**, 불확실성 붕괴·richness 용량·인과 국소화 다루지 않음 |
| **arXiv:2602.21054** (VAUQ) | 비전-인지 불확실성 측정(마스킹 IS), 훈련 불필요 | **계측·독립 근거 교차검증으로 채택**, 경쟁 방법 아님 (증류·인과 국소화 없음) |

→ 신규성 = "관련 연구가 없다"가 아니라 **방어 가능한 델타**: 위 4개 모두 검증된 실재 논문이며, 어느 것도 *용량-통제 + 혼란변수-차감 + 온-매니폴드 검증 + 부위 인과 국소화*를 동시에 하지 않는다.

## 6. 실험 / 평가

- **데이터셋**: MathVista, MathVision, ChartQA, ScienceQA-IMG, DocVQA. **손상 격자**: ImageNet-C 15종 × 5단계 + 인페인팅 표적 증거 제거.
- **기저선**: ① 무증류 base(혼란변수, 2504.03440 재현) ② Arm2 플라시보 ③ 대칭 자기증류(s=0) ④ 상관-only 국소화(매개가 푸는 것을 상관이 못 풂을 입증).
- **지표**: 용량-반응 기울기(단조성 검정) · 증류-기인 효과크기(부트스트랩 CI) · ECE/AUROC/과신율 · 의미적 엔트로피 · VAUQ IS · 부위별 매개 비율+가법성 격차 · 유효 랭크 추이 · 근거 탐침 특이도(관련 제거−크기맞춤 대조) · in-domain vs OOD 격차.
- **어블레이션**: 교차어텐션 하드 마스킹(폐기된 선행 방법)이 온-매니폴드 탐침과 다른/불안정한 국소화를 줌을 시연; 3지표 삼각측정 일치.

## 7. 기대 기여

1. VLM판 conditioning-richness(깨끗/손상 비대칭)를 **연속 조작 인과 용량**으로 정의·조작 — *증상*이 아니라 *메커니즘*을 이식.
2. 플라시보 arm + 무증류 기저선으로 **증류-기인·richness-구동 성분을 기저 손상 효과에서 분리**.
3. 동일-폭 시퀀스 자기증류가 시각 인코더 유효 랭크를 좁히는지 **가정 대신 실증** (선행 "외과적 레버" 연구가 빌려온 전제를 제거).
4. 가법성 검정된 인과 매개로 붕괴를 **시각 인코더 vs 투영기 vs 언어 헤드**에 귀속 — 상관이 못 가리는 질문 해결.
5. 교차어텐션/오프-매니폴드 아티팩트에 강건한 근거 테스트.

## 8. 한계와 향후 과제

- 결론은 언어화 불확실성이 추출 가능한 오픈 ~7B 추론형 VLM에 한정; 폐쇄형·비추론형은 다를 수 있음.
- 활성값 패칭은 개입이 모델 자연 활성 매니폴드 위에 있다고 가정; 그래디언트 결합에 의한 **심한 비가법성이면 깔끔한 부위 귀속이 제한**됨(사전등록된 음성 결과).
- "깨끗한 이미지 = 모범 정답"은 *가장 가까운* 아날로그지 완벽한 일치 아님 — 모범 정답은 학생이 *생성할 표적*이고 깨끗한 이미지는 *더 풍부한 입력 증거*. (리뷰어 다수가 지적한 약점.)
- 손상 강도는 정보량의 대리변수; 의미적 손실과 저수준 손실을 완전히 분리하지 못함.
- 유효 랭크는 여러 차원성 대리지표 중 하나; 랭크 음성이 다른 기하 메커니즘을 배제하진 않음.

**사전등록된 정직한 음성 결과(모두 발표 가치 있음).** ⒜ 유효 랭크가 안 좁혀짐 → 랭크-붕괴 서사 무효(용량-반응/부위 결과는 독립 보고). ⒝ 효과가 주로 언어 헤드 매개 → 시각-특이 부위 주장 실패, 메커니즘이 대체로 모달리티-불가지. ⒞ 기저 혼란변수 차감 후 증류-기인 효과가 0과 구별 불가 → VLM 역설은 대부분 알려진 기저 효과. ⒟ 강한 부위 비가법성 → 매개가 깨끗이 분해 못 함.

## 9. 리뷰어 점수표 (실제 패널, 보정된 7=견고한 accept)

| 리뷰어 | 신규성 | 견고성 | 중요성 | 명료성 | 종합 | 추천 |
|---|---|---|---|---|---|---|
| R1 | 8 | 7 | 7 | 6 | **7** | weak_accept |
| R2 | 8 | 7 | 7 | 6 | **7** | weak_accept |
| R3 | 8 | 7 | 7 | 6 | **7** | weak_accept |
| R4 | 8 | 6 | 7 | 5 | **7** | weak_accept |
| **평균** | **8.00** | 6.75 | 7.00 | 5.75 | **7.00** | — |

- **치명적 결함: 0** (4인 전원 명시).
- **통과 판정: 엄격 통과선 미달** — 평균 7.0 < 7.5 (그 외 최저≥7·신규성≥7.5·결함0은 충족). **다듬으면 통과 가능한 깔끔한 weak-accept.**
- **반복 지적된 약점 = 다음 개정 방향**: ① "깨끗한 이미지 ↔ 모범 정답" 아날로그가 느슨함(지각 증거 vs 추론 표적) — 메커니즘 동일성을 *주장*이 아니라 *논증/실증*으로 격상 필요. ② 매개 분해의 가법성 가정이 취약(그래디언트 결합) — 실패 시 경로-특이 효과/상호작용항 폴백 명시 필요. ③ 중요성 천장: 직관적 가설의 확인·국소화에 그칠 위험 → **실행 가능한 완화책(mitigation)** 으로 연결하면 중요성 점수 상승 여지. ④ 명료성(평균 5.75)이 최저 축 — 제목·표기 단순화 필요.

---

## 부록: 인용 검증 (2026-06-05 수행)

워크플로 자동 합성이 후반 크래시로 실패하여, **본 보고서는 트랜스크립트에서 복구한 최고점 제안서**이며 모든 부담 인용을 arXiv 원문 대조로 직접 검증했다.

| arXiv ID | 검증된 제목 | 저자(확인) | 검증 결과 |
|---|---|---|---|
| 2603.24472 | Why Does Self-Distillation (Sometimes) Degrade the Reasoning Capability of LLMs? | Jeonghye Kim, Xufang Luo, Minbeom Kim 외 | ✅ 실재·설명 정확 (원천 메커니즘) |
| 2504.03440 | Know What You Do Not Know: Verbalized Uncertainty… on Corrupted Images in VLMs | (제목 확인, 초록 미수신) | ✅ 제목 일치 |
| 2602.21054 | VAUQ: Vision-Aware Uncertainty Quantification for LVLM Self-Evaluation | Seongheon Park, Changdae Oh, Sharon Li 외 | ✅ 실재·설명 정확 (Image-Information Score) |
| 2310.08255 | Leveraging Vision-Language Models for Improving Domain Generalization in Image Classification (VL2V-ADiP) | Sravanti Addepalli, R. Venkatesh Babu 외 | ✅ 실재 (제안서가 라벨 ‘VL2V-SD’와 실제 제목 차이 및 증류 방향 반대임을 정확히 명시) |

**부담 인용 4/4 모두 실재·정확 확인.** 신규성 주장은 검증된 선행연구 위에 서 있으며, 어떤 미검증 논문에도 기대지 않는다. 단, 이는 *측정된 결과가 아니라 제안서*이며 — 모든 예측(용량-반응 단조 붕괴, 시각 인코더 매개)은 아직 실증되지 않은 가설임을 명시한다.
